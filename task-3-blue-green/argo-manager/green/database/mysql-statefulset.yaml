apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mysql-green
  namespace: database
spec:
  serviceName: mysql
  replicas: 1
  selector:
    matchLabels:
      app: mysql
      version: v2 # ✅ ADDED: Version label for green deployment
  template:
    metadata:
      labels:
        app: mysql
        version: v2 # ✅ ADDED: Version label for green deployment
    spec:
      containers:
        - name: mysql
          image: mysql:8.0
          ports:
            - containerPort: 3306
              name: mysql
          envFrom:
            - secretRef:
                name: mysql-secret-green # ✅ UPDATED: Reference to the green secret
          volumeMounts:
            - name: mysql-data
              mountPath: /var/lib/mysql
            # ✅ ADDED: Mount point for the shared Filestore volume (Task 2)
            - name: shared-storage
              mountPath: /var/www/shared-data
      
      # ✅ ADDED: Reference to the external Filestore PVC
      volumes:
        - name: shared-storage
          persistentVolumeClaim:
            claimName: mysql-pvc-green # ✅ UPDATED: Reference to the green PVC

  volumeClaimTemplates:
    - metadata:
        name: mysql-data
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 10Gi
